import { AppRoutePlaceholder } from '@/components/shared/AppRoutePlaceholder';

function LeadsHomePage() {
	return (
		<AppRoutePlaceholder
			description="Destino inicial previsto para atendentes após autenticação."
			title="Meus Leads"
		/>
	);
}

export default LeadsHomePage;
