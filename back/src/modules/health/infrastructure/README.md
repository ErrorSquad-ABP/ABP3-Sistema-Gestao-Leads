# Infrastructure do módulo health

Este módulo ainda não possui infraestrutura própria porque o health check atual é puramente operacional.

Quando necessário, esta camada deve receber adaptadores específicos do módulo, sem criar uma infraestrutura global fora de `shared`.
