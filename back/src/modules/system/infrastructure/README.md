# Infrastructure do módulo system

Esta camada deve receber apenas integrações próprias do módulo `system`, como adapters de observabilidade, versionamento ou endpoints institucionais.

Infraestrutura compartilhada entre módulos deve permanecer em `src/shared`.
